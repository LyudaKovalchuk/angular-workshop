import { RegisterTemplateDirective } from './register-template.directive';

describe('RegisterTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
